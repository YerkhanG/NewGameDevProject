namespace persistence_system.model
{
    //this will probably the current run save??/ idk tho. Since every run the player state is new.
    public class PlayerState
    {
        public int maxHealth;
        public int health;
        public int baseDamage;
    }
}